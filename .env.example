# ============================================
# DATABASE CONFIGURATION - MySQL 8.0+
# ============================================

# ⚠️ IMPORTANTE: Esta aplicación requiere MySQL (no SQLite)
# El schema.prisma está configurado para MySQL con índices FULLTEXT y ENUMs

# DESARROLLO LOCAL:
DATABASE_URL="mysql://amistoso_app:app_password@localhost:3306/amistoso_ter_db?connection_limit=10&pool_timeout=20&connect_timeout=10"

# PRODUCCIÓN (recomendado con SSL):
# DATABASE_URL="mysql://user:pass@host:3306/db?connection_limit=20&pool_timeout=30&connect_timeout=10&ssl=true&sslaccept=strict"

# PARÁMETROS DE CONNECTION POOLING:
# - connection_limit: Máximo de conexiones simultáneas (default: 10, recomendado: 10-20)
# - pool_timeout: Tiempo máximo esperando conexión disponible en segundos (default: 10)
# - connect_timeout: Tiempo máximo para establecer conexión inicial en segundos (default: 5)
# - ssl: Habilitar SSL/TLS (OBLIGATORIO en producción)
# - sslaccept: Modo de validación SSL (strict = validar certificado)

# PROVEEDORES CLOUD RECOMENDADOS:
# 
# PlanetScale (Serverless MySQL, pool automático):
# DATABASE_URL="mysql://user:pass@aws.connect.psdb.cloud/db?sslaccept=strict"
# 
# Railway (MySQL 8.0):
# DATABASE_URL="mysql://root:pass@containers-us-west-123.railway.app:6789/railway?connection_limit=15"
# 
# AWS RDS (MySQL 8.0):
# DATABASE_URL="mysql://admin:pass@mydb.xxxxx.us-east-1.rds.amazonaws.com:3306/amistoso?ssl=true&connection_limit=20"
# 
# Digital Ocean Managed MySQL:
# DATABASE_URL="mysql://doadmin:pass@db-mysql-nyc3-12345-do-user.db.ondigitalocean.com:25060/defaultdb?ssl=true"

# ============================================
# NEXTAUTH CONFIGURATION
# ============================================

NEXTAUTH_URL="http://localhost:3000"

# ⚠️ CRÍTICO: Cambiar en producción
# Generar nuevo secret seguro con: openssl rand -base64 32
NEXTAUTH_SECRET="changeme-insecure-development-only-secret"

# Producción:
# NEXTAUTH_URL="https://tu-dominio.com"
# NEXTAUTH_SECRET="<generar con openssl rand -base64 32>"

# ============================================
# SECURITY & RATE LIMITING
# ============================================

# Rate limiting (configurado en lib/rate-limit.ts)
# Estos valores son opcionales, hay defaults en el código

# RATE_LIMIT_WINDOW_MS=900000        # 15 minutos en ms
# RATE_LIMIT_MAX_REQUESTS=100        # Requests generales
# RATE_LIMIT_AUTH_MAX_REQUESTS=10    # Requests de auth

# CORS (para producción, configurar dominios permitidos)
# ALLOWED_ORIGINS="https://tu-frontend.com,https://www.tu-frontend.com"

# ============================================
# LOGGING & MONITORING
# ============================================

# Nivel de logs
# LOG_LEVEL="info"  # opciones: debug, info, warn, error

# Sentry (opcional, para error tracking)
# SENTRY_DSN="https://xxx@xxx.ingest.sentry.io/xxx"

# ============================================
# NODE ENVIRONMENT
# ============================================

# ============================================
# GOOGLE ADSENSE (Dashboard)
# ============================================
# Publisher ID (ej: ca-pub-1234567890123456)
NEXT_PUBLIC_ADSENSE_CLIENT=""

# Slots recomendados
NEXT_PUBLIC_ADSENSE_DASHBOARD_SLOT=""
NEXT_PUBLIC_ADSENSE_DASHBOARD_INLINE_SLOT=""
NEXT_PUBLIC_ADSENSE_DASHBOARD_MULTIPLEX_SLOT=""
NEXT_PUBLIC_ADSENSE_DASHBOARD_MULTIPLEX_LAYOUT=""
NEXT_PUBLIC_ADSENSE_REQUESTS_INLINE_SLOT=""
NEXT_PUBLIC_ADSENSE_REQUESTS_MULTIPLEX_SLOT=""
NEXT_PUBLIC_ADSENSE_REQUESTS_MULTIPLEX_LAYOUT=""

# NODE_ENV="development"  # Automático en `npm run dev`
# En producción: NODE_ENV="production"
